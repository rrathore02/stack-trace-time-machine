# Copy to .env and fill in your values.

# GitHub personal access token (only needed for `stt revert` to open a draft PR).
# Required scopes: repo
GITHUB_TOKEN=

# Default repo (owner/name) to open PRs against. Optional.
GITHUB_REPO=
